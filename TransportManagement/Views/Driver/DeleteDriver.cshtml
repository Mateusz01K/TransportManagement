@model TransportManagement.Models.Drivers.DriverViewModel
@{
    ViewData["Title"] = "Kierowcy";
}

<div class="text-center">
    <h1 class="display-4">Lista Kierwców</h1>
    <div class="table-container">
        <table class="table table-hover" id="usersTable">
            <thead>
                <tr>
                    <th scope="col">ID</th>
                    <th scope="col">Imie</th>
                    <th scope="col">Nazwisko</th>
                    <th scope="col">Data Urodzenia</th>
                    <th scope="col">Nr Telefonu</th>
                    <th scope="col">Email</th>
                    <th scope="col">Adres</th>
                    <th scope="col">Doświadczenie</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model.Drivers)
                {
                    <tr>
                        <td scope="row">@item.Id</td>
                        <td scope="row">@item.Name</td>
                        <td scope="row">@item.LastName</td>
                        <td scope="row">@item.DateOfBirth</td>
                        <td scope="row">@item.PhoneNumber</td>
                        <td scope="row">@item.Email</td>
                        <td scope="row">@item.Address</td>
                        <td scope="row">@item.Experience</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
<form method="delete" asp-controller="Driver" asp-action="DeleteThisDriver" class="form-container">
    <div class="form-group">
        <label for="Id">Id: <span style="color: red">*</span></label>
        <input type="number" class="form-control" id="Id" placeholder="Id" name="Id" required />
    </div>

    <div class="text-center m-5">
        <button type="submit" class="btn btn-danger">Usuń</button>
    </div>
    <div class="text-center mt-4">
        <a class="btn btn-danger" href="javascript: history.go(-1)">Wróć</a>
    </div>
</form>
